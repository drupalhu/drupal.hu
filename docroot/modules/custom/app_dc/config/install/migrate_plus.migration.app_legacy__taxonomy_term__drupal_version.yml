langcode: en
status: true
id: app_legacy__taxonomy_term__drupal_version
label: 'App - Legacy - taxonomy_term:drupal_version'

migration_tags:
  - app
  - legacy_content

migration_group: app_legacy

source:
  plugin: d7_taxonomy_term
  bundle:
    - vocabulary_10
  constants:
    status: true
    format: html_teaser

process:
  tid: tid
  status: constants/status
  parent: parent
  weight: weight
  name: name
  description/value: description
  description/format: constants/format

destination:
  plugin: 'entity:taxonomy_term'
  default_bundle: drupal_version
